# AI DAST Scanner - Environment Configuration
# Copy this file to .env and add your API keys:
#   cp .env.example .env
#
# ============================================================================
# LLM PROVIDER CONFIGURATION
# ============================================================================

# Which LLM provider to use: ollama, openrouter, or openai
# Default: ollama
LLM_PROVIDER=ollama

# ----------------------------------------------------------------------------
# Ollama Configuration (when LLM_PROVIDER=ollama)
# ----------------------------------------------------------------------------

# Model to use (e.g., llama3, qwen3, mistral)
# If not set, auto-selects from available models
OLLAMA_MODEL=

# Ollama server host URL
OLLAMA_HOST=http://localhost:11434

# Request timeout in seconds
OLLAMA_TIMEOUT=500

# Maximum retry attempts for failed requests
OLLAMA_MAX_RETRIES=3

# ----------------------------------------------------------------------------
# OpenRouter Configuration (when LLM_PROVIDER=openrouter)
# ----------------------------------------------------------------------------

# OpenRouter API key (required) - get yours at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Model to use (e.g., google/gemma-3-12b-it:free, anthropic/claude-3.5-sonnet)
# See available models at https://openrouter.ai/models
OPENROUTER_MODEL=

# ----------------------------------------------------------------------------
# OpenAI Configuration (when LLM_PROVIDER=openai)
# ----------------------------------------------------------------------------

# OpenAI API key (required) - get yours at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Model to use (default: gpt-4o)
OPENAI_MODEL=gpt-4o

# ============================================================================
# SCANNER CONFIGURATION (Optional)
# ============================================================================

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

